# M1-T11 — Build pipeline orchestration & planner.db.lock.json

## Goal

Implement `src/npc_planner/ingest/build.py` orchestrating full database creation (`planner.db`) from raw baseline dumps and writing the `planner.db.lock.json` manifest.

## Files you may create or modify

- `src/npc_planner/ingest/build.py`
- `tests/unit/test_ingest_build.py`

## Files you may read but NOT modify

- `src/npc_planner/db/session.py`
- `src/npc_planner/ingest/baseline.py`
- `src/npc_planner/ingest/provenance.py`

## Files you may NOT touch

Everything else. Verify with `git diff --name-only` before committing.

## Contract

```python
# src/npc_planner/ingest/build.py
from pathlib import Path

def build_database(
    raw_dir: Path | str,
    db_out: Path | str,
    lock_out: Path | str | None = None
) -> Path:
    """Build pure SQLite planner.db from raw baseline dumps and generate lock.json. Returns db path."""
    ...
```

## Must-pass tests

1. `build_database("tests/fixtures/m1_tiny", tmp_db_path, tmp_lock_path)` builds database file and lock file.
2. `planner.db.lock.json` contains valid build metadata and table counts matching staging tables.
3. Database build is pure and reproducible (identical inputs yield identical lock hashes).

## Out of scope — do NOT implement

- CLI commands — that is **M1-T12**

## Acceptance command

```bash
make check && TMP_DIR=$(mktemp -d) && python3 -c "
from pathlib import Path
from npc_planner.ingest.build import build_database

db_path = build_database('tests/fixtures/m1_tiny', Path('$TMP_DIR') / 'planner.db')
print(db_path.exists())
" && rm -rf "$TMP_DIR"
# expected output: True
```

## Definition of Done

- [ ] All 3 must-pass tests written **first**, observed failing, then passing
- [ ] `make check` green (lint, mypy --strict, schema-check, unit, integration)
- [ ] `git diff --name-only` shows only the allowed files
- [ ] No function returns a placeholder value
- [ ] Commit message: `feat(ingest): build pipeline orchestration and lockfile   [M1-T11]`
- [ ] `docs/LEDGER.md` row updated to `done` with the commit sha
