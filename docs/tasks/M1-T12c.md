# M1-T12c — Ingest default value cleanup, #if/#elif C initializers, coverage audit regex, LEDGER & issue trace

Address review findings:
1. Drop fabricated defaults in `moves.py` (`type`, `effect`, `target` -> default `None`, raise/record missing fields explicitly).
2. Handle `#if`/`#elif`/`#else`/`#endif` inside C array initializers in `cparse.py`.
3. Make `audit_moves_coverage` regex top-level aware to avoid false-positive nested keys like `.moveEffect` inside macro calls.
4. Update LEDGER.md with `M1-T12c` row and open GitHub issues trace.

## Files you may create or modify

- `src/npc_planner/ingest/cparse.py`
- `src/npc_planner/ingest/moves.py`
- `tests/unit/test_moves_ingest.py`
- `tests/unit/test_cparse_expr.py`
- `scripts/accept/M1-T12b_checks.py`
- `docs/LEDGER.md`
