# M1-T08b - Remediate M1-T08: consume resolve_rom_repo, adopt shared fixtures

## Goal

Make `scripts/preprocess_rom.py` fall back to `resolve_rom_repo()` when `--repo` is omitted, refuse to run against a repo that fails a hard doctor check, and switch `tests/unit/test_rom_probe.py` onto the shared fixtures from `M1-T00b`.

## Files you may create or modify

- `scripts/preprocess_rom.py`
- `src/npc_planner/ingest/rom_probe.py`
- `tests/unit/test_rom_probe.py`
- `src/npc_planner/config.py`
- `docs/tasks/M1-T08b.md`

## Files you may read but NOT modify

- `src/npc_planner/environment.py`
- `tests/unit/conftest.py`
- `docs/PREREQUISITES.md`
- `docs/ROM_SOURCING.md`

## Files you may NOT touch

Everything else.

## Contract

- `preprocess_rom.py --output data/raw/romdata/` -> repo resolved via `environment.resolve_rom_repo()`
- `preprocess_rom.py --repo <path> --output <dir>` -> explicit path wins
- Exit 2 if the repo cannot be resolved (`RomRepoNotFoundError`).
- Exit 3 if the repo resolves but a hard doctor check fails; print failing check names and remedy strings, stop without writing any `.i` files.
- `rom_probe.probe_layout` does NOT import `environment`.

## Definition of Done

- [ ] Step 0 findings recorded in the commit body
- [ ] All 7 must-pass tests written **first**, observed failing, then passing
- [ ] `make check` green (lint, mypy --strict, schema-check, unit, integration)
- [ ] `git diff --name-only` shows only the allowed files
- [ ] Exactly one implementation of the four-level precedence rule exists in the codebase
- [ ] `rom_probe.py` does not import `npc_planner.environment`
- [ ] No `.i` file is ever written for a tree that fails a hard check
- [ ] Commit message: `fix(ingest): route repo resolution through environment   [M1-T08b]`
- [ ] `docs/LEDGER.md` row added and set to `done` with the commit sha
