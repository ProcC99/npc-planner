# M1-T00b - Remediate M1-T00: severity model, exit contract, shared fixtures

## Goal

Make the hard/soft severity model explicit and machine-checkable, define `doctor.py`'s exit contract, pin the expansion-vs-vanilla discriminator to a named constant, and move the git and no-network fixtures into a shared `conftest.py` so `M1-T08` can use them.

## Files you may create or modify

- `src/npc_planner/environment.py`
- `scripts/doctor.py`
- `tests/unit/conftest.py`
- `tests/unit/test_environment.py`
- `.gitignore`
- `docs/tasks/M1-T00b.md`

## Files you may read but NOT modify

- `docs/PREREQUISITES.md`
- `docs/tasks/M1-T00.md`
- `tests/fixtures/fake_rom/**`

## Files you may NOT touch

Everything else.

## Contract

```python
# src/npc_planner/environment.py

CHECK_SEVERITY: dict[str, Severity] = {
    "cpp":               "hard",
    "repo_present":      "hard",
    "is_git_repo":       "hard",
    "expansion_markers": "hard",
    "upstream_remote":   "soft",
    "tree_clean":        "soft",
}

EXPANSION_MARKERS: tuple[str, ...] = (
    "include/config/species_enabled.h",
    "include/config/battle.h",
)
```

## Exit Contract for scripts/doctor.py

- Exit 0: every hard check passed.
- Exit 1: at least one hard check failed.
- Soft failures print a warning line and DO NOT affect the exit code.
- Exit 2: --repo omitted and resolve_rom_repo() raised RomRepoNotFoundError.

## Definition of Done

- [ ] All 11 must-pass tests written **first**, observed failing, then passing
- [ ] `make check` green (lint, mypy --strict, schema-check, unit, integration)
- [ ] `git diff --name-only` shows only the allowed files
- [ ] `grep -rn "os.environ" src/npc_planner/environment.py` returns nothing
- [ ] Commit message: `fix(config): explicit severity model and shared env fixtures   [M1-T00b]`
- [ ] `docs/LEDGER.md` row added and set to `done` with the commit sha
