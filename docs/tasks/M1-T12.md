# M1-T12 — CLI stub, data build & pokemon show commands

## Goal

Implement the Typer CLI entrypoint in `src/npc_planner/cli/main.py` with commands `data build` and `pokemon show <species_or_form>`.

## Files you may create or modify

- `src/npc_planner/cli/__init__.py`
- `src/npc_planner/cli/main.py`
- `tests/unit/test_cli_main.py`

## Files you may read but NOT modify

- `src/npc_planner/ingest/build.py`
- `src/npc_planner/db/session.py`

## Files you may NOT touch

Everything else. Verify with `git diff --name-only` before committing.

## Contract

```python
# src/npc_planner/cli/main.py
import typer

app = typer.Typer(help="NPC Team Planner CLI")

@app.command()
def build(raw_dir: str = "data/raw/gen3_official", db_out: str = "planner.db") -> None:
    """Build SQLite database from raw baseline dumps."""
    ...

@app.command()
def pokemon_show(identifier: str) -> None:
    """Display information about a Pokémon species or form."""
    ...
```

## Must-pass tests

1. `npc-planner --help` executes cleanly and returns exit code 0.
2. `npc-planner data build --raw-dir tests/fixtures/m1_tiny --db-out <tmp_db>` builds database.
3. `npc-planner pokemon show skarmory` displays Skarmory details (types, stats, species info).

## Out of scope — do NOT implement

- `diff-baseline` command — that is **M2-T04**
- `explain` command — that is **M3-T08**

## Acceptance command

```bash
make check && python3 -m npc_planner.cli.main --help && python3 -m npc_planner.cli.main pokemon show skarmory
```

## Definition of Done

- [ ] All 3 must-pass tests written **first**, observed failing, then passing
- [ ] `make check` green (lint, mypy --strict, schema-check, unit, integration)
- [ ] `git diff --name-only` shows only the allowed files
- [ ] No function returns a placeholder value
- [ ] Commit message: `feat(cli): Typer CLI stub with data build and pokemon show   [M1-T12]`
- [ ] `docs/LEDGER.md` row updated to `done` with the commit sha
