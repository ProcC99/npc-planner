# M1-T11c - Symbol value preservation in unparsed_fields and acceptance probe fixes

**Commit subject:** `fix(ingest): record symbol values in unparsed_fields and fix acceptance probes   [M1-T11c]`

Four remediations based on the M1-T11b review:

1. **Card Unmodified Check:** Update `card_unmodified` check in acceptance scripts to use `CARD_COMMIT` relative diffing rather than `milestone/M1...HEAD`.
2. **Behavioral Probe for Done Predicate:** Replace string-matching in `probe_done_predicate_has_no_sha_conjunct()` with a behavioral test (constructing a fixture with `status = done` and `sha = —` and verifying rejection & placeholder note).
3. **Step 0 Diagnosis Settlement:** Log exact shas (`2fe6d25` task commit vs `26debbd` ledger commit).
4. **Dex Symbol Value Preservation:** When `natDexNum` is symbolic, store `national_dex = None` and record `natDexNum=SYMBOL_NAME` (e.g., `natDexNum=NATIONAL_DEX_SKARMORY`) in `unparsed_fields`.

## Files you may create or modify

- `src/npc_planner/ingest/species.py`
- `tests/unit/test_species_ingest.py`
- `scripts/hooks/task_id_required.py`
- `scripts/accept/M1-T11b.sh`
- `scripts/accept/M1-T11b_checks.py`
- `scripts/accept/M1-T11c.sh`
- `scripts/accept/M1-T11c_checks.py`
- `docs/LEDGER.md`

`docs/tasks/M1-T11c.md` is **not** in this list. Do not edit this card.
