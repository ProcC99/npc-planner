# M1-T05 — SQLite session factory, WAL mode & foreign keys

## Goal

Implement SQLite database connection factory and context manager in `src/npc_planner/db/session.py` that configures WAL mode, enables foreign keys (`PRAGMA foreign_keys = ON;`), and executes `schema.sql` on database initialization.

## Files you may create or modify

- `src/npc_planner/db/__init__.py`
- `src/npc_planner/db/session.py`
- `tests/unit/test_db_session.py`

## Files you may read but NOT modify

- `src/npc_planner/db/schema.sql`
- `src/npc_planner/config.py`

## Files you may NOT touch

Everything else. Verify with `git diff --name-only` before committing.

## Contract

```python
# src/npc_planner/db/session.py
import sqlite3
from contextlib import contextmanager
from pathlib import Path
from typing import Generator

def create_connection(db_path: Path | str = ":memory:") -> sqlite3.Connection:
    """Connect to SQLite database, configure WAL mode and enable foreign keys."""
    ...

def init_db(conn: sqlite3.Connection) -> None:
    """Execute schema.sql to initialize database tables and views."""
    ...

@contextmanager
def get_db(db_path: Path | str = ":memory:") -> Generator[sqlite3.Connection, None, None]:
    """Context manager providing a managed database connection."""
    ...
```

## Must-pass tests

1. `create_connection(":memory:")` returns connection with `PRAGMA foreign_keys` enabled (`1`).
2. `init_db(conn)` initializes all 31 tables and 2 views.
3. `get_db(db_path)` context manager yields working connection and closes cleanly on exit.

## Out of scope — do NOT implement

- Pydantic models — that is **M1-T06**
- Baseline loader or build pipeline — that is **M1-T09 / M1-T11**

## Acceptance command

```bash
make check && python3 -c "
from npc_planner.db.session import get_db, init_db
with get_db(':memory:') as conn:
    init_db(conn)
    fk = conn.execute('PRAGMA foreign_keys').fetchone()[0]
    tables = conn.execute(\"SELECT COUNT(*) FROM sqlite_master WHERE type='table'\").fetchone()[0]
    print(fk, tables)
"
# expected output: 1 31
```

## Definition of Done

- [ ] All 3 must-pass tests written **first**, observed failing, then passing
- [ ] `make check` green (lint, mypy --strict, schema-check, unit, integration)
- [ ] `git diff --name-only` shows only the allowed files
- [ ] No function returns a placeholder value
- [ ] Commit message: `feat(db): SQLite session factory and WAL PRAGMA   [M1-T05]`
- [ ] `docs/LEDGER.md` row updated to `done` with the commit sha
