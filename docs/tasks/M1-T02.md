# M1-T02 — Envelope[T] generic response model & unit tests

## Goal

Implement generic `Envelope[T]` Pydantic model in `src/npc_planner/models/envelope.py` for structured query responses containing `query`, `context`, `results`, `excluded`, `warnings`, and `meta`.

## Files you may create or modify

- `src/npc_planner/models/__init__.py`
- `src/npc_planner/models/envelope.py`
- `tests/unit/test_envelope.py`

## Files you may read but NOT modify

- `src/npc_planner/config.py`
- `EXECUTION_PROTOCOL.md`

## Files you may NOT touch

Everything else. Verify with `git diff --name-only` before committing.

## Contract

```python
# src/npc_planner/models/envelope.py
from typing import Generic, TypeVar, Any
from pydantic import BaseModel, Field

T = TypeVar("T")

class ExcludedReason(BaseModel):
    key: str
    reasons: list[dict[str, Any]] | list[str]

class Meta(BaseModel):
    count: int = 0
    elapsed_ms: float = 0.0
    deterministic_hash: str | None = None

class Envelope(BaseModel, Generic[T]):
    query: dict[str, Any] = Field(default_factory=dict)
    context: dict[str, Any] = Field(default_factory=dict)
    results: T
    excluded: list[ExcludedReason] = Field(default_factory=list)
    warnings: list[str] = Field(default_factory=list)
    meta: Meta = Field(default_factory=Meta)
```

## Must-pass tests

1. `Envelope[list[str]]` initializes with results and default metadata.
2. Serialization to JSON includes all fields (`query`, `context`, `results`, `excluded`, `warnings`, `meta`).
3. Deserialization from JSON round-trips correctly.

## Out of scope — do NOT implement

- Any SQL database models — that is **M1-T06**
- Any CLI commands — that is **M1-T12**

## Acceptance command

```bash
make check && python3 -c "
from npc_planner.models.envelope import Envelope
env = Envelope[list[str]](results=['a', 'b'])
print(env.results, env.meta.count)
"
# expected output: ['a', 'b'] 0
```

## Definition of Done

- [ ] All 3 must-pass tests written **first**, observed failing, then passing
- [ ] `make check` green (lint, mypy --strict, schema-check, unit, integration)
- [ ] `git diff --name-only` shows only the allowed files
- [ ] No function returns a placeholder value
- [ ] Commit message: `feat(models): generic envelope response model   [M1-T02]`
- [ ] `docs/LEDGER.md` row updated to `done` with the commit sha
