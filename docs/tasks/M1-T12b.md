# M1-T12b — Fix Fixture, CExpr, Name Extraction, Protocol Rules

**Commit subject:** `feat(ingest): fixture fidelity, CExpr, COMPOUND_STRING name fix   [M1-T12b]`

## Items

1. **Fix fixture `.split` → `.category`, rewrite to upstream conventions:**
   - `COMPOUND_STRING` for names (not `_()`), `TARGET_*` (not `MOVE_TARGET_*`), all standard fields.
   - Add `MOVE_TOXIC` (status, verified upstream) and `MOVE_WILL_O_WISP` (non-derivable name, verified upstream).
   - Remove `"split"` from `MAPPED_FIELDS`, delete `.split` fallback.
   - Root-cause: fixture (§11.16 — `.split` does not exist in upstream ROM).

2. **Restore `--reverse | head -1` for `CARD_COMMIT` across all suites.**
   - T12 card mutation (`401de73ef52c01f5bf71f7fefa45b24893593953`) asserted via `audit_commits.py`.

3. **Record M1-T12 violations in `audit_commits.py` structured violations list.**

4. **Replace `card_touched_once` with `expect_exit 0` branch-point form in T11b, T11c.**
   - Removal reason: `card_touched_once` was wrong — cards may be revised before branching.

5. **`CExpr` variant in `cparse.py` + duplicate-key detection:**
   - Structural classification: int → `int`, single identifier → `str`, anything else → `CExpr`.
   - `CExpr(raw, identifiers)` with `is_constant` property.
   - String-literal stripping before identifier extraction.
   - Duplicate key in `_parse_inner_braces` → `CParseError`.

6. **Protocol rules §11.15 (failing check is evidence), §11.16 (fixture fidelity).**
   - §11.15 requires `Root-cause:` trailer and new-card for widening.
   - §11.16: `fake_rom` is faithful to upstream; synthetic edge cases go inline.

7. **`COMPOUND_STRING` name extraction (discovered in Step 0):**
   - Existing `.name` parser only handles `_()` and `g()` macros; `COMPOUND_STRING` falls through to rom_id derivation, making all name assertions vacuous for moves with derivable names.
   - Add `"COMPOUND_STRING"` to macro name check.
   - Add arity guard: `len(args) != 1` → raise `MoveParseError`.
   - `MOVE_WILL_O_WISP` ("Will-O-Wisp" vs derivation "Will O Wisp") discriminates.
   - Root-cause: code (name macro check was incomplete).
   - Species is NOT broken — `_()` is unwrapped to plain `str` by `_clean_string_literal` at parse time.

**All mapped `str` fields become `X | None`** due to `CExpr` appearing in any upstream field.

**`unevaluated_fields: tuple[tuple[str, str], ...]`** on both `SpeciesRecord` and `MoveRecord` (no default). Three-way disjointness + sortedness invariant in `__post_init__`.

**`unparsed_fields` stores keys only** — no nested-value serialization concern.

**T12c scope (named, not executed):** `#if`/`#elif`/`#endif` handling. Duplicate-key `CParseError` will fire on real upstream (162 in moves, 375 in species). Both parsers blocked on real upstream until T12c.

## Files you may create or modify

- `src/npc_planner/ingest/cparse.py`
- `src/npc_planner/ingest/moves.py`
- `src/npc_planner/ingest/species.py`
- `tests/fixtures/fake_rom/src/data/moves_info.h`
- `tests/unit/test_moves_ingest.py`
- `tests/unit/test_species_ingest.py`
- `tests/unit/test_cparse_expr.py`
- `scripts/audit_commits.py`
- `scripts/accept/M1-T11.sh`
- `scripts/accept/M1-T11b.sh`
- `scripts/accept/M1-T11c.sh`
- `scripts/accept/M1-T12.sh`
- `scripts/accept/M1-T12b.sh`
- `scripts/accept/M1-T12b_checks.py`
- `docs/LEDGER.md`
- `EXECUTION_PROTOCOL.md`

`docs/tasks/M1-T12b.md` is not in this list and must not be edited after the initial commit.
