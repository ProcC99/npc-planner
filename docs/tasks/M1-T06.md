# M1-T06 — Pydantic row models for 26 domain tables

## Goal

Implement Pydantic row models in `src/npc_planner/db/models.py` corresponding to the 26 domain tables defined in `schema.sql` (e.g. `BuildInfo`, `Provenance`, `TypeRow`, `TypeMatchup`, `Species`, `Form`, `Ability`, `Move`, `Learnset`, `TrainerSpec`, `GeneratedTeam`, `ValidationIssue`, etc.).

## Files you may create or modify

- `src/npc_planner/db/models.py`
- `tests/unit/test_db_models.py`

## Files you may read but NOT modify

- `src/npc_planner/db/schema.sql`
- `docs/schema_manifest.txt`

## Files you may NOT touch

Everything else. Verify with `git diff --name-only` before committing.

## Contract

```python
# src/npc_planner/db/models.py
from pydantic import BaseModel, Field
from typing import Optional, Any

class BuildInfoRow(BaseModel): ...
class ProvenanceRow(BaseModel): ...
class TypeRow(BaseModel): ...
class TypeMatchupRow(BaseModel): ...
class SpeciesRow(BaseModel): ...
class FormRow(BaseModel): ...
class AbilityRow(BaseModel): ...
class MoveRow(BaseModel): ...
class LearnsetRow(BaseModel): ...
class TrainerSpecRow(BaseModel): ...
class GeneratedTeamRow(BaseModel): ...
class GeneratedTeamMemberRow(BaseModel): ...
class GeneratedMovesetRow(BaseModel): ...
class ValidationIssueRow(BaseModel): ...
```

## Must-pass tests

1. Models map 1-to-1 to column names and types in `schema.sql`.
2. Valid dictionary payloads validate cleanly for core models (`FormRow`, `MoveRow`, `AbilityRow`, `TrainerSpecRow`).
3. Model validation fails on missing required fields or invalid enum choices.

## Out of scope — do NOT implement

- Staging table loaders — that is **M1-T09**
- YAML overlay parser — that is **M2-T01**

## Acceptance command

```bash
make check && python3 -c "
from npc_planner.db.models import FormRow, MoveRow
f = FormRow(form_id='skarmory', species_slug='skarmory', type_1='steel', type_2='flying', base_hp=65, base_atk=80, base_def=140, base_spa=40, base_spd=70, base_spe=70)
m = MoveRow(move_slug='thunderbolt', move_name='Thunderbolt', type_slug='electric', damage_class='special')
print(f.form_id, m.damage_class)
"
# expected output: skarmory special
```

## Definition of Done

- [ ] All 3 must-pass tests written **first**, observed failing, then passing
- [ ] `make check` green (lint, mypy --strict, schema-check, unit, integration)
- [ ] `git diff --name-only` shows only the allowed files
- [ ] No function returns a placeholder value
- [ ] Commit message: `feat(db): Pydantic row models for domain tables   [M1-T06]`
- [ ] `docs/LEDGER.md` row updated to `done` with the commit sha
