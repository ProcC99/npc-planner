# M1-T12d — Fix CI workflow branch filter, revert C preprocessor regex, restore frozen T12b check, fix test_hooks.py python executable resolution, and create M1-T12d acceptance suite

Address review findings from M1-T12c:
1. Fix `.github/workflows/gate.yml` branch trigger to `[main, "milestone/**"]`.
2. Revert `#if` preprocessor stripping in `cparse.py` and replace with explicit `CParseError` detection when preprocessor directives appear inside array initializers.
3. Restore `scripts/accept/M1-T12b_checks.py` to its exact frozen state from M1-T12b.
4. Replace `.venv/bin/python3` in `tests/unit/test_hooks.py` with `sys.executable` for environment independence.
5. Create `scripts/accept/M1-T12d.sh` and `scripts/accept/M1-T12d_checks.py` acceptance suite.
6. Update `docs/LEDGER.md` with `M1-T12d` row.

## Files you may create or modify

- `.github/workflows/gate.yml`
- `src/npc_planner/ingest/cparse.py`
- `scripts/accept/M1-T12b_checks.py`
- `tests/unit/test_hooks.py`
- `tests/unit/test_cparse_expr.py`
- `scripts/accept/M1-T12d.sh`
- `scripts/accept/M1-T12d_checks.py`
- `docs/LEDGER.md`
