# M1-T04 — Staging tables, views & schema manifest verification

## Goal

Add 5 staging tables (`stg_species`, `stg_forms`, `stg_moves`, `stg_abilities`, `stg_type_matchups`) and 2 views (`v_form_full`, `v_battle_confidence`) to `src/npc_planner/db/schema.sql`, passing full `check_schema_manifest.py` validation.

## Files you may create or modify

- `src/npc_planner/db/schema.sql`
- `scripts/check_schema_manifest.py`
- `tests/unit/test_schema_staging_views.py`

## Files you may read but NOT modify

- `docs/schema_manifest.txt`

## Files you may NOT touch

Everything else. Verify with `git diff --name-only` before committing.

## Must-pass tests

1. `schema.sql` creates all 31 tables (26 domain + 5 staging) and 2 views against in-memory SQLite database.
2. `python3 scripts/check_schema_manifest.py` passes with zero warnings or errors.
3. `v_form_full` and `v_battle_confidence` views can be queried without SQL syntax errors.

## Out of scope — do NOT implement

- Session connection factory — that is **M1-T05**
- Baseline loader writing to `stg_*` — that is **M1-T09**

## Acceptance command

```bash
make check && python3 scripts/check_schema_manifest.py
# expected output: schema manifest OK: 26 domain + 5 staging + 2 views
```

## Definition of Done

- [ ] All 3 must-pass tests written **first**, observed failing, then passing
- [ ] `make check` green (lint, mypy --strict, schema-check, unit, integration)
- [ ] `git diff --name-only` shows only the allowed files
- [ ] No function returns a placeholder value
- [ ] Commit message: `feat(db): staging tables and views DDL   [M1-T04]`
- [ ] `docs/LEDGER.md` row updated to `done` with the commit sha
