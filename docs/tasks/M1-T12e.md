# M1-T12e — Environment and suite integrity

Fix CI gate workflow, enforce frozen acceptance counts and deterministic Python resolution in `_lib.sh`, relocate in-tree ROM clone, and add `doctor.py` guard.

## Tasks

1. **CI Gate Fix**: Update `.github/workflows/gate.yml` `Install hooks` step to run `make hooks` (installing both `pre-commit` and `commit-msg` hook shims) so `check_hooks_installed.py` succeeds on GitHub Actions runner.
2. **Interpreter Resolution & Count Enforcement**: Update `scripts/accept/_lib.sh` to:
   - Export `$REPO_ROOT/.venv/bin` into `$PATH` and resolve `PY` to `$REPO_ROOT/.venv/bin/python3` if present.
   - Extend `accept_summary` with an optional `expected_total` argument that fails if total executed checks differs from expected count.
   - Pass frozen expected totals to `accept_summary` in `M1-T11.sh` (33), `M1-T11b.sh` (32), `M1-T11c.sh` (30), `M1-T12b.sh` (23), `M1-T12d.sh` (5).
3. **Relocate ROM clone & add guards**:
   - Move in-tree `pokeemerald-expansion/` to `/home/jpurple/workish/pokeemerald-expansion`.
   - Add `pokeemerald-expansion/` to `.gitignore`.
   - Add guard in `scripts/doctor.py` to fail if resolved ROM repository path is inside the planner working tree.
4. **Acceptance Suite**: Create `scripts/accept/M1-T12e.sh` and `scripts/accept/M1-T12e_checks.py`.

## Protocol Amendment Notice

Per human direction, updating `_lib.sh` and adding count assertions to `accept_summary` in frozen acceptance scripts is a shared infrastructure improvement to guarantee deterministic Python selection and prevent silent check drop.

## Files you may create or modify

- `.github/workflows/gate.yml`
- `.gitignore`
- `scripts/accept/_lib.sh`
- `scripts/accept/M1-T11.sh`
- `scripts/accept/M1-T11b.sh`
- `scripts/accept/M1-T11c.sh`
- `scripts/accept/M1-T12.sh`
- `scripts/accept/M1-T12b.sh`
- `scripts/accept/M1-T12d.sh`
- `scripts/accept/M1-T12e.sh`
- `scripts/accept/M1-T12e_checks.py`
- `scripts/doctor.py`
- `src/npc_planner/environment.py`
- `tests/unit/test_environment.py`
- `docs/LEDGER.md`
- `docs/tasks/M1-T12e.md`
