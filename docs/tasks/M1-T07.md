# M1-T07 — Fake pokeemerald-expansion repo fixture tree

## Goal

Create a minimal offline test fixture tree representing a pokeemerald-expansion repository in `tests/fixtures/fake_rom/`. This tree will serve as the offline source for unit tests across layout probing, preprocessing, parsing, and extractors.

## Files you may create or modify

- `tests/fixtures/fake_rom/include/constants/expansion.h`
- `tests/fixtures/fake_rom/include/config/battle.h`
- `tests/fixtures/fake_rom/include/config/pokemon.h`
- `tests/fixtures/fake_rom/include/config/species_enabled.h`
- `tests/fixtures/fake_rom/src/data/pokemon/species_info.h`
- `tests/fixtures/fake_rom/src/data/moves_info.h`
- `tests/fixtures/fake_rom/src/data/abilities.h`
- `tests/fixtures/fake_rom/src/data/types_info.h`
- `tests/fixtures/fake_rom/src/data/items.h`
- `tests/fixtures/fake_rom/src/data/pokemon/level_up_learnsets.h`
- `tests/fixtures/fake_rom/src/data/pokemon/teachable_learnsets.h`
- `tests/fixtures/fake_rom/src/data/pokemon/egg_moves.h`
- `tests/fixtures/fake_rom/src/data/wild_encounters.json`
- `tests/fixtures/fake_rom/src/data/trainers.party`
- `tests/unit/test_fake_rom_fixture.py`

## Files you may read but NOT modify

- `docs/ROM_SOURCING.md`

## Files you may NOT touch

Everything else. Verify with `git diff --name-only` before committing.

## Must-pass tests

1. All required C headers and JSON/party files in `tests/fixtures/fake_rom/` exist and can be read.
2. `species_info.h` contains sample species definitions (e.g. `SPECIES_SKARMORY` and a Gen 9 guarded species).
3. `wild_encounters.json` parses as valid JSON.
4. `trainers.party` contains valid Showdown-like party definitions for Gym Leader Marlon.

## Out of scope — do NOT implement

- `cpp` preprocessing execution script — that is **M1-T08**
- C initializer parser — that is **M1-T09**

## Acceptance command

```bash
make check && python3 -c "
from pathlib import Path
p = Path('tests/fixtures/fake_rom')
headers = list(p.glob('**/*.h'))
print(len(headers))
"
# expected output: 12
```

## Definition of Done

- [ ] All 4 must-pass tests written **first**, observed failing, then passing
- [ ] `make check` green (lint, mypy --strict, schema-check, unit, integration)
- [ ] `git diff --name-only` shows only the allowed files
- [ ] No function returns a placeholder value
- [ ] Commit message: `feat(test): author fake_rom test fixture tree   [M1-T07]`
- [ ] `docs/LEDGER.md` row updated to `done` with the commit sha
